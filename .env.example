# =============================================================================
# Variables d'environnement — Système de pourboire Stripe
# =============================================================================
# Copiez ce fichier en .env.local et remplissez les valeurs.
# Ne commitez jamais .env ou .env.local (ils sont dans .gitignore).
# =============================================================================

# -----------------------------------------------------------------------------
# Base de données PostgreSQL
# -----------------------------------------------------------------------------
# Format : postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Exemple local : postgresql://postgres:postgres@localhost:5432/pourboire
# Exemple cloud (Neon, Supabase, etc.) : fourni par le provider
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pourboire"

# -----------------------------------------------------------------------------
# Stripe — Clés API
# -----------------------------------------------------------------------------
# Clé secrète (Secret Key) : utilisée côté serveur uniquement.
# Dashboard Stripe : Développeurs > Clés API > Clé secrète.
# En test : commence par sk_test_...
# En production : commence par sk_live_...
STRIPE_SECRET_KEY="sk_test_..."

# Clé publique (Publishable Key) : peut être exposée côté client (pour Stripe.js).
# En test : pk_test_...
# En production : pk_live_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# -----------------------------------------------------------------------------
# Stripe — Webhook
# -----------------------------------------------------------------------------
# Signing secret du webhook : permet de vérifier que les événements viennent bien de Stripe.
# Dashboard : Développeurs > Webhooks > [votre endpoint] > Signing secret.
# En local : utilisez Stripe CLI pour obtenir un secret temporaire (whsec_...).
STRIPE_WEBHOOK_SECRET="whsec_..."

# -----------------------------------------------------------------------------
# URL de base (optionnel)
# -----------------------------------------------------------------------------
# Utilisé pour les URLs de succès/annulation dans la Checkout Session.
# En dev : http://localhost:3000
# En prod : https://votredomaine.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"
